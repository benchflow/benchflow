logging:
  level: Info
  loggers:
    cloud.benchflow.datamanager: DEBUG
    com.google.api.client.http: Warn

swagger:
  resourcePackage: cloud.benchflow.datamanager.service.resources

minio:
  address: ${env.MINIO_ADDRESS!"SET_ME"}
  accessKey: ${env.MINIO_ACCESS_KEY!"SET_ME"}
  secretKey: ${env.MINIO_SECRET_KEY!"SET_ME"}
  defaultBucket: ${env.MINIO_DEFAULT_BUCKET!"SET_ME"}

googleDrive:
  applicationName: ${env.GOOGLE_DRIVE_APPNAME!"SET_ME"}
  secret: ${env.GOOGLE_DRIVE_SECRET!"SET_ME"}
  credentialsDir: ${env.GOOGLE_DRIVE_CREDENTIALS_DIR!"SET_ME"}
  baseFolderName: ${env.GOOGLE_DRIVE_BASEFOLDER!"SET_ME"}

cassandra:
  host: ${env.CASSANDRA_HOST!"localhost"}
  keyspace: ${env.CASSANDRA_KEYSPACE!"benchflow"}
  username: ${env.CASSANDRA_USERNAME!""}
  password: ${env.CASSANDRA_PASSWORD!""}

