engines:
  checkstyle:
    enabled: true
    channel: "beta"
  scalastyle:
    enabled: true
  markdownlint:
    enabled: true
  shellcheck:
    enabled: true

ratings:
  paths:
    - "**.java"
    - "**.scala"
    - "Dockerfile"
    - "Dockerfile.ci"
    - "Dockerfile.ci"

exclude_paths:
- "**/src/test/**"
